/** 所有用到的 CSS custom properties key，方便集中管理 */
export const TOKENS = {
    baseHeight: "--gx-base-height",
    barGap: "--gx-bar-gap",
    radius: "--gx-radius",
    barDuration: "--gx-bar-duration",
    spinnerSize: "--gx-spinner-size",
    barColor: "--gx-bar-bc",
    bars: "--gx-bars",
    spinnerColor: "--gx-spinner-color",
} as const;